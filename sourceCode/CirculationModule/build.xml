<project name="Circulation" default="deploy" basedir=".">

    <!-- Define properties -->
    <property name="Appmanage" value="C:\tibco\tra\5.7\bin\AppManage.exe" />
    <property name="build.dir" value="${basedir}/Circulation/Deployables" />
    <property name="app.name" value="Circulation" />
<property name="project.bw.application" value="${basedir}/Circulation" />
    <!-- Define targets -->
	
   

    <target name="deploy" >
        <exec executable="${Appmanage}">
            <arg line="-deploy --propFile C:\tibco\tra\5.7\bin\AppManage.tra -app ${app.name} -ear ${build.dir}/EAR/Circulation.ear -deployconfig ${build.dir}/Conf/CirculationConfig.xml -user admin -pw admin -domain admin" />
        </exec>
    </target>

</project>
